<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".view.profile.CameraFragment">

    <ImageButton
        android:id="@+id/camera_capture_button"
        android:layout_width="@dimen/fragment_50_dp"
        android:layout_height="@dimen/fragment_50_dp"
        android:layout_marginBottom="@dimen/fragment_50_dp"
        android:scaleType="fitCenter"
        android:src="@drawable/take_photo"
        android:backgroundTint="@color/leaf"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:elevation="@dimen/fragment_2_dp"/>

    <ImageButton
        android:id="@+id/button_save_photo"
        android:visibility="gone"
        android:layout_width="@dimen/fragment_50_dp"
        android:layout_height="@dimen/fragment_50_dp"
        android:layout_marginBottom="@dimen/fragment_50_dp"
        android:scaleType="fitCenter"
        android:layout_marginEnd="@dimen/fragment_150_dp"
        android:src="@drawable/ic_check"
        android:backgroundTint="@color/leaf"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:elevation="@dimen/fragment_2_dp"/>

    <ImageButton
        android:id="@+id/button_cancel"
        android:visibility="gone"
        android:layout_width="@dimen/fragment_50_dp"
        android:layout_height="@dimen/fragment_50_dp"
        android:layout_marginBottom="@dimen/fragment_50_dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_cancel"
        android:backgroundTint="@color/leaf"
        android:layout_marginStart="@dimen/fragment_150_dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:elevation="@dimen/fragment_2_dp"/>

    <androidx.camera.view.PreviewView
        android:id="@+id/view_finder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_preview"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>